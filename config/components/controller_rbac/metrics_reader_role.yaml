apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kata-provider
    app.kubernetes.io/managed-by: kustomize
  name: kata-provider-metrics-reader
rules:
- nonResourceURLs:
  - "/metrics"
  verbs:
  - get
