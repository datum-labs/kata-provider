# The Kubernetes RuntimeClass that points the kubelet at the Kata handler. It is
# not Datum's runtime class: this is the node-level runtime binding that a
# general-purpose instance's Pod names, and it must exist in the cluster before
# any such Pod can be admitted.
#
# The handler name must match the provider's runtimeHandler setting. Both name
# Cloud Hypervisor, which is what kata-deploy installs as kata-clh. An arm64
# cluster has no Cloud Hypervisor shim, so it changes both to kata-qemu.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: kata-clh
  labels:
    app.kubernetes.io/name: kata-provider
    app.kubernetes.io/managed-by: kustomize
handler: kata-clh
scheduling:
  nodeSelector:
    katacontainers.io/kata-runtime: "true"
