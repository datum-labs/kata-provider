apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: kata-provider
    app.kubernetes.io/name: kata-provider
    app.kubernetes.io/managed-by: kustomize
  name: kata-provider-metrics
spec:
  ports:
    - name: metrics
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    control-plane: kata-provider
    app.kubernetes.io/name: kata-provider
